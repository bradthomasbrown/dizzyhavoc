include:
  # - kanta/compose.yaml
  - node/compose.yaml
services:
  test:
    build:
      context: ../..
      dockerfile: tests/chain-w-dzhv/Dockerfile
    environment:
      - DEPLOYER_SECRET
      - IMPLEMENTER_SECRET
      - DESTROYER_SECRET
      - WALLET_SECRET
    depends_on:
      # - kanta
      - node
    command: >
      deno run -A setup.ts